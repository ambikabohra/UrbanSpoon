// module.exports.getUserDetails = function(req, res) 
// {
//     var location= (req.body.location).trim();
//     var db = req.db;
//     var collection = db.get('users'); //collection name, in which restaurant dataset is stored
// }

// collection.find({ email: 216});
